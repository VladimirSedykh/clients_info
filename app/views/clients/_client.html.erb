<tr>
  <td> <%= link_to client.name, client_path(client)  %> </td>
  <td> <%= client.description%> </td>
  <td> <%= client.short_contacts %> </td>
  <td> <%= client.created_at.strftime("%m/%d/%Y %H:%M:%S")%> </td>
</tr>
